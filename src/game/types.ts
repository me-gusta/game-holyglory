export type Hero = {
    label: string
    is_unlocked: boolean
    name: string
    level: string
    bio: string
}

export type Spell = {
    label: string
    is_unlocked: boolean
    name: string
    level: string
    about: string
}
